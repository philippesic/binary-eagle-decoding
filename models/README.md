# Local model artifacts

Store downloaded snapshots in `hf/`, converted models in `gguf/`, and training
checkpoints in `checkpoints/`. All are ignored. Record immutable upstream
revisions, hashes, licenses, and conversion settings with each experiment.
Model weights have not been downloaded as part of repository setup.
